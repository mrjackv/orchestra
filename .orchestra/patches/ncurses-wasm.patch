--- a/ncurses/Makefile
+++ b/ncurses/Makefile
@@ -230,7 +230,7 @@
 	$(SHELL) -e $(base)/MKlib_gen.sh "$(CPP) $(CPPFLAGS)" "$(AWK)" generated <../include/curses.h >$@
 
 init_keytry.h: make_keys$(BUILD_EXEEXT) keys.list
-	./make_keys$(BUILD_EXEEXT) keys.list > $@
+	node make_keys$(BUILD_EXEEXT) keys.list > $@
 
 keys.list :	$(tinfo)/MKkeys_list.sh
 	AWK=$(AWK) $(SHELL) $(tinfo)/MKkeys_list.sh $(CAPLIST) | LC_ALL=C sort >$@
@@ -251,7 +251,7 @@
 		$(srcdir)/curses.priv.h \
 		$(srcdir)/report_offsets.c
 	$(BUILD_CC) -o $@ $(BUILD_CPPFLAGS) $(BUILD_CCFLAGS) $(srcdir)/report_offsets.c $(BUILD_LDFLAGS) $(BUILD_LIBS)
-	./report_offsets$(BUILD_EXEEXT)
+	node report_offsets$(BUILD_EXEEXT)
 
 ./expanded.c : $(srcdir)/curses.priv.h $(serial)/MKexpanded.sh
 	$(SHELL) -e $(serial)/MKexpanded.sh "$(CPP)" $(CPPFLAGS) > $@
--- a/ncurses/tinfo/MKcaptab.sh
+++ b/ncurses/tinfo/MKcaptab.sh
@@ -71,8 +71,8 @@
 /* *INDENT-OFF* */
 EOF
 
-cat "$@" |./make_hash 1 info $OPT1
-cat "$@" |./make_hash 3 cap  $OPT1
+cat "$@" | node make_hash 1 info $OPT1
+cat "$@" | node make_hash 3 cap  $OPT1
 
 cat "$@" |$AWK -f $OPT2 bigstrings=$OPT1 tablename=capalias
 
--- a/ncurses/tinfo/MKuserdefs.sh
+++ b/ncurses/tinfo/MKuserdefs.sh
@@ -51,7 +51,7 @@
 #if NCURSES_XNAMES
 EOF
 
-cat "$@" | ./make_hash 1 user $OPT1
+cat "$@" | node make_hash 1 user $OPT1
 
 cat <<EOF
 
--- a/misc/Makefile	2022-07-15 16:59:37.553241422 +0200
+++ b/misc/Makefile	2022-07-15 17:01:02.083239401 +0200
@@ -90,17 +90,6 @@
 		$(DESTDIR)$(libdir) \
 		$(DESTDIR)$(datadir) \
 		$(DESTDIR)$(tabsetdir)
-	DESTDIR=${DESTDIR} \
-	prefix=${prefix} \
-	exec_prefix=${exec_prefix} \
-	bindir=${bindir} \
-	top_srcdir=${top_srcdir} \
-	srcdir=${srcdir} \
-	datadir=${datadir} \
-	ticdir=${ticdir} \
-	source=terminfo.tmp \
-	cross_compiling=no \
-	$(SHELL) ./run_tic.sh
 	@cd $(srcdir)/tabset && \
 		$(SHELL) -c 'for i in * ; do \
 			if test -f $$i ; then \
